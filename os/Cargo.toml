[package]
name = "os"
version = "0.1.0"
edition = "2024"

[dependencies]
lazy_static = { version = "1.4", features = ["spin_no_std"] }
log = "0.4.27"
sbi-rt = { version = "0.0.2", features = ["legacy"] }
